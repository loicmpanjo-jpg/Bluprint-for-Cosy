# ============================================================
# COSY V0.1 — Render Blueprint (CORRIGÉ)
# Documentation: https://render.com/docs/blueprint-spec
# ============================================================
# CHANGEMENTS:
# - cron: plan supprimé (free non supporté, minimum $1/mois)
# - worker: plan supprimé (free non supporté)
# - static sites: runtime: static au lieu de type: static
# - Redis: type: keyvalue dans services
# - JWT keys: sync: false (générer manuellement)
# ============================================================

services:
  # --- API Backend (Fastify) ---
  - type: web
    name: cosy-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    autoDeploy: false
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: cosy-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: cosy-redis
          property: connectionString
      - key: JWT_PRIVATE_KEY
        sync: false
      - key: JWT_PUBLIC_KEY
        sync: false
      - key: TWILIO_ACCOUNT_SID
        sync: false
      - key: TWILIO_AUTH_TOKEN
        sync: false
      - key: TWILIO_PHONE_NUMBER
        sync: false
      - key: APP_NAME
        value: COSY
      - key: APP_VERSION
        value: "0.1"
      - key: CORS_ORIGIN
        value: "https://cosy-app.onrender.com,https://cosy-dashboard.onrender.com"
      - key: RATE_LIMIT_WINDOW
        value: "60000"
      - key: RATE_LIMIT_MAX
        value: "100"
      - key: OTP_EXPIRY
        value: "300"
      - key: JWT_EXPIRY
        value: "3600"
      - key: REFRESH_TOKEN_EXPIRY
        value: "604800"
      - fromGroup: cosy-shared

  # --- Frontend SPA (Static) ---
  - type: web
    name: cosy-app
    runtime: static
    buildCommand: echo "Static SPA - no build needed"
    staticPublishPath: ./frontend
    envVars:
      - key: API_URL
        value: https://cosy-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # --- Dashboard Admin (Static) ---
  - type: web
    name: cosy-dashboard
    runtime: static
    buildCommand: echo "Static Dashboard - no build needed"
    staticPublishPath: ./dashboard
    envVars:
      - key: API_URL
        value: https://cosy-api.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # --- Background Worker (Queue) ---
  # NOTE: plan: free n'est PAS supporté pour les workers
  # Options: 1) Supprimer plan (default: starter $7/mois)
  #          2) Intégrer le worker dans l'API web service
  - type: worker
    name: cosy-worker
    runtime: node
    buildCommand: npm install
    startCommand: npm run worker
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cosy-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: keyvalue
          name: cosy-redis
          property: connectionString
      - fromGroup: cosy-shared

  # --- Cron Job (Daily stats) ---
  # NOTE: plan: free n'est PAS supporté pour les cron jobs
  # Minimum: $1/mois (facturé à la seconde d'exécution)
  # Alternative: utiliser un service externe comme cron-job.org (gratuit)
  - type: cron
    name: cosy-cron
    runtime: node
    schedule: "0 0 * * *"
    buildCommand: npm install
    startCommand: npm run cron
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cosy-db
          property: connectionString
      - fromGroup: cosy-shared

  # --- Redis Key Value ---
  - type: keyvalue
    name: cosy-redis
    ipAllowList: []
    maxmemoryPolicy: allkeys-lru

databases:
  - name: cosy-db
    databaseName: cosy
    user: cosy_admin
    plan: free
    postgresMajorVersion: "15"
    ipAllowList: []

envVarGroups:
  - name: cosy-shared
    envVars:
      - key: APP_NAME
        value: COSY
      - key: APP_VERSION
        value: "0.1"
      - key: RATE_LIMIT_WINDOW
        value: "60000"
      - key: RATE_LIMIT_MAX
        value: "100"
